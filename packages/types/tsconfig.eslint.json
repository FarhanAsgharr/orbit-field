{
  "//": "ESLint-only type information: the build config excludes tests, type-aware linting wants them.",
  "extends": "./tsconfig.json",
  "compilerOptions": { "noEmit": true },
  "include": ["src/**/*.ts"],
  "exclude": ["dist"]
}
